shoplist=['apple', 'mange', 'carrot']
name = 'swaroop'
print(shoplist[0])#这里方框中表示的是要输出第几个，从零开始计数，【-1】指的是最后一个项目，
# 【-2】指的是倒数第二个项目【：】返回的是整个序列，【1：3】表示从位置一开始到位置三结束【：-1】表示强返回一组序列不包括
#序列最后一个项目
#字符串#str 字符串在程序中所有使用的字符都是字符串，
name = 'swaroop' #一个字符串对象字符串用于查找是否已给定的内容开头，in检查是字符串中的一部分
if name.startswith('swa'):
    print('')
if 'a' in name:
    print('')
if name.find('war') != -1:
    print('')
#" // "来表示整数除法，返回不大于结果的一个最大的整数

d = discr > 0
if d == Ture:

s = 'woshixiaoxiannu'
for sim in s:
print(sim)  # 打印出来是字符串中每一个元素，这里必须是sim



















