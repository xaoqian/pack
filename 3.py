c = 50
h = 30
d = input('Введите d через запятые， но без пробелов: ')
f = d.split(',')#split通过指定分隔符对字符串进行切片，如果参数有指定值，那么只分分隔子字符串
for k in range(0, len(f)):
    s = int( f[k] )
    q = ((2 * c * s) /h)**(1/2)
    print(q,',')#结果用逗号隔开

